# Dark Echo — 実装タスクリスト（TODO）

> **このファイルの役割**: 壁打ち（企画会議）で「実装する」と決まった**全機能**を、
> セッションが切り替わっても忠実に再現できる解像度で記録する**唯一の実装管理台帳**。
> - `CLAUDE.md` = プロジェクト概要・確定仕様・クイックリファレンス（自動読込）
> - `HANDOVER.md` = セッションログ・決定経緯・パフォーマンス教訓
> - **`TODO.md`（本書）= 何を作るか / どこまで作ったか**
>
> **作業の鉄則**:
> 1. 着手前に本書で該当項目の**ステータスとコード位置**を必ず確認する。
> 2. 実装したら**ステータスと検証日**を更新し、コード位置（`game.js:行`）を追記する。
> 3. 設計が複数ファイルで矛盾する場合は**時系列で新しい方を採用**（下の「設計コンフリクト解決」参照）。
> 4. 「設計どおり実装済み」と早合点しない。本書の📍コード位置で実物を確認してから判断する。

**ステータス凡例**: ✅ 実装済 / 🚧 部分実装 / ❌ 未実装 / 🧊 構想のみ（仕様未確定）

**最終コード検証**: 2026-06-14 / **`origin/main`（PR #34〜#41マージ済み）基準**

> ⚠️ **来歴（2026-06-14）**: 旧開発枝 `claude/t01-implementation-start-dauj7q` は PR #35〜#38 を欠く
> 取り残し枝だった（PR #34 は**マージ済み**）。ドキュメント3点を現mainから切り直して統合し直し、
> 実装状況を最新mainコードで再検証済み（PR#40）。続いて4機能を実装（PR#41）。
> §1の行番号の一部は旧コード基準の概数が残るため、実装着手時は実物を再確認すること。
> マージ済みPR: #34=有視界+スプライト / #35=方位ウェッジ+予測敵AI / #36=艦種シルエット+噴射色 /
> #37=スプライト+DPR / #38=星雲パララックス / #39→#40に統合 / #40=MD3本統合 /
> #41=想定ロック完成+敵AI適応+ジャミング3種+デコイ+積載量。

> 📎 **出典表記について**: 本書中の `game_design_v2.md §X` / `MEMORY.md:行` 等の参照は、
> **2026-06-14に廃止した旧設計書の出典**（内容は本書に転記済み）。原典は git 履歴に保存。

---

## 0. 設計コンフリクト解決（新しい方を正とする）

壁打ちの時系列で仕様が上書きされた箇所。**以下を唯一の正**とし、旧記述は無効。

| 項目 | 旧記述（無効） | **採用する最新仕様** | 根拠 |
|---|---|---|---|
| マップサイズ | 8000×8000 / 50000×50000 | **円形マップ `MAP_RADIUS=35000`（直径70000）** | コード実態 `game.js:35` |
| 勝利条件 | 「敵撃破のみ・拠点なし」 | **2モード制（BR=殲滅 / S&D=全コロニーハック or 殲滅）** | `gameState.mode` 実装済 |
| ドローン実装クラス | `TalosDrone` クラス | **`fighter`型 `Ship`** で代用（TalosDroneクラスは廃止） | 現コードに該当クラス無し |
| 物質名 | エレボス (Erebos) | **ヒッグス (Higgs)** に全面リネーム済 | rename完了 |
| タイトル | Dark Current / Dark Signal | **Dark Echo**（最終）。リポジトリ名は `Dark-Higgs` | 決定ログ |
| TALOS出撃(50CR) | 旧HANDOVER記述 | **廃止**。現行は艦種別の固有ユニット（敵carrierのfighterスポーン等） | コード実態 |

---

## 1. ✅ 実装済み（壁打ち項目・検証済）

> 「未実装」と誤解されがちだが**実際に動いている**もの。再実装しないこと。

### 索敵・センサー
- ✅ **有視界システム** — `computeVisionRadius()` @`game.js:201`。ヒッグス濃度連動でアメーバ状に縮小（0%=基準1200, 100%でも最低5%=`MIN_VISION_FACTOR`）。`updateVisionLockOn()` @`game.js:294`。**PR#34**
- ✅ **自動ロックオン + 攻撃ON/OFF** — `autoAttackEnabled` @`game.js:192`、`btn-attack-toggle` @`game.js:2740`。視野内最近傍を自動ターゲット @`game.js:350`
- ✅ **武器射程サークル** — 選択武器の射程円を自機周りに描画 @`game.js:4646`（missile=2200 / beam=8000 / kinetic=800）
- ✅ **完全ロックオン** — 視野内の敵を高精度コンタクト化 @`game.js:339`
- ✅ **センサー4種（じゃんけん方式）** — `currentSensor` heat/optic/em/higgs
- ✅ **パッシブ方位ウェッジ（"運ゲー"解消・PR#35）** — `checkPassiveDetection()`が方位扇形を生成。`passiveBearings[]`/`drawPassiveBearings()` @main`game.js:50-53,3069-3102`。強信号=狭い確信/弱信号=広いボケ、計測位置にアンカー、**移動して2回計測→三角測量で敵位置を絞れる**。8秒フェード・最大2-8本
- ✅ **アクティブソナー（指向性含む）** — `fireOmniSonar()` @`game.js:2869`。CD15秒。方位°表示あり @`game.js:3035`
- ✅ **スレットリング方角表示** — 敵方角付近を膨らませる24セグメント弧 @`game.js:4201`
- ✅ **オシロスコープ** — `updateSigCanvas()`。H/O/E/G 4チャネル波形
- ✅ **EM ∝ AI配分** — `playerEmBoost=0.5+ai/100*0.5` @`game.js:1532`、敵探知係数 `aiCoeff` @`game.js:2953`

### GEN / リソース
- ✅ **GEN 4スライダー（ゼロサム100%）** — `genAlloc{engine,weapons,sensors,ai}`
- ✅ **GEN→発射レート係数** — `weaponGenFactor=max(0.3, 1.5-weapons/100)` @`game.js:1365`
- ✅ **リソースノード** — `resourceNodes[]`。HIGGSセンサーのみ可視、収集でEMスパイク+SCR
- ✅ **アップグレード** — `buyUpgrade()` @`game.js:4763`。`engine/weapons/armor/sensor` の4種Lv0-3（`UPGRADE_MULT=[_,1.0,1.5,2.0]`）。※設計ツリーとズレ→ §3-1

### 武器
- ✅ **武器3種** — kinetic/missile/beam + シグネチャ（kinetic=光学 / missile=熱+誘導EM / beam=チャージ熱+EM・発射光学+ダークチャネル）
- ✅ **マガジン・リロード** — `kineticAmmo=8`、`KINETIC_RELOAD_TIME=180`/`MISSILE=300`/`BEAM=240` @`game.js:1203-1262`
- ✅ **攻撃型3連装同時発射** — kinetic時のみ @`game.js:1354`
- ✅ **ビームダークチャネル** — ヒッグス雲を通ると筋が残る @`game.js:1100`
- ✅ **先制攻撃ボーナス** — 非警戒の敵に2xダメージ @`game.js:1139`

### ヒッグス
- ✅ **ヒッグス濃度（0-100%連続値）** — `getHiggsIntensity()`（フレームキャッシュ付）
- ✅ **ヒッグスウェイク軌跡** — `higgsWakes[]`、HIGGSセンサーで追跡可
- ✅ **自然成長（ゾーン圧縮）** — 時間経過で濃度上昇 @`game.js:4251`
- ✅ **エンジン減速 / ビームダメージ減衰** — 高濃度内で速度低下・ビーム威力低下

### 艦種・エンジン・描画
- ✅ **艦種3種** — assault(HP3500,×0.8) / stealth(HP700,×1.4) / carrier(HP2500,×0.6) @`game.js:1220,1302`
- ✅ **艦種別シルエット（PR#36）** — `gameState.shipType`で自機描画を分岐（assault=重厚戦艦 / stealth=細い暗色ステルス / carrier=横長母艦）
- ✅ **エンジン4種特性** — `ENGINE_TYPES` thermonuclear/pulse/higgs/photon（熱・光学・EM倍率 + 速度ボーナス）@`game.js:62`
- ✅ **エンジン種別→噴射トレイル色（PR#36）** — `ENGINE_THRUST`/`drawThruster()` @main`game.js:78,1908-1928`（thermonuclear=橙〜白 / pulse=青紫 / higgs=暗紫ほぼ不可視 / photon=純白〜青白）。全艦種共通
- ✅ **生成スプライト + 高DPR対応（PR#37/#38）** — 自機3種/敵4種/ノード/コロニー/難破船をHiggsfield生成スプライト化。DPRボケ修正。星雲スクリーン空間パララックス層化

### モード・敵AI・構造物
- ✅ **2モード** — BR（殲滅）/ S&D（全コロニーハック or 殲滅）。コロニー数 S&D=5 / BR=3 @`game.js:2528`
- ✅ **敵4状態AI** — lurking/gathering/hunting/combat @`game.js:1243`
- ✅ **敵ユニット4種** — corvette/destroyer/carrier/fighter（fighter=ドローン）。敵carrierが`fighter`を周期スポーン @`game.js:1546`
- ✅ **コロニー / 難破船** — colony=S&Dハック目標、derelict=EWハックで**偽コンタクト発生**（`decoyActive`）@`game.js:880,2840`
- ✅ **生成スプライト表示** — `SPRITES` 非同期ローダー、未ロード時ベクター描画フォールバック。**PR#34**

### 基盤
- ✅ HUDオーバーレイ（ズーム不変）/ ミニマップ / セーブロード / ゲームオーバー・クリア画面 / カメラ追従 / スマホ操作（ドラッグ・長押し・ピンチ）

### 移動・戦闘メカニクス（2026-06-15実装 PR#76）
- ✅ **重慣性物理システム** — `currentSpeed` プロパティ（初期値0）による2層速度設計。`_baseTargetSpeed`（目標）に向けて `SHIP_ACCEL_RATE` で加速。停止時は `×3` で素早く減速。
  - `SHIP_MAX_SPEED_MULT = { assault: 0.58, stealth: 1.05, carrier: 0.38 }` — 最高速倍率
  - `SHIP_ACCEL_RATE = { carrier: 0.003, assault: 0.008, stealth: 0.016 }` — 加速レート
  - `SHIP_TURN_SLOW = { carrier: 0.78, assault: 0.52, stealth: 0.22 }` — 旋回中の速度低下率（旋回量 `|diff|/(π×0.12)` で正規化した係数を掛ける）
  - `PLAYER_TURN_RATES = { assault: 0.010, stealth: 0.015, carrier: 0.004 }` — 旋回レート(rad/frame)
- ✅ **武器射角（fire arc）** — `WEAPON_FIRE_ARC = { kinetic: Math.PI*5/6, missile: Math.PI/4, beam: Math.PI/18 }`（kinetic=±150° / missile=±45° / beam=±10°）。自動発砲前に `|diff| < _fireArc` を判定。
- ✅ **右クリック自由射撃（フリーファイア）** — ミサイル/ビームのみ。`contextmenu` イベントでワールド座標変換後、射角チェック→発砲。外れると自位置シグネチャが露出（ミサイル=熱 / ビーム=ヒッグスダークチャネル+EMスパイク）。
- ✅ **後方被弾ボーナス（×1.5）** — Projectile.update で被弾角度が `|diff| > π×0.55`（後方弧）の時 `dmgMult *= 1.5`。自機被弾時はフロートテキスト「後方被弾 ×1.5」表示。

### デモモード・ロビー追加（2026-06-15実装 PR#76+77）
- ✅ **デモモード** — `demoMode` フラグ（初期 `false`）。`drawFogOfWar()` と `updateVisionLockOn()` を早期 return でバイパス→全フィールド・全敵が常時見える。敵AIの挙動は通常と同一。`toggleDemoMode()` 関数 + `btn-demo-mode`（トップバー）でトグル。
- ✅ **敵艦種選択（ロビー）** — `gameState.enemyType: 'assault'|'stealth'|'carrier'`。ロビーに `.enemy-select-btn` 3択を追加。`generateSector()` で内部型にマップ（`assault→destroyer / stealth→corvette / carrier→carrier`）してボス敵をスポーン。

---

## 2. 🚧 部分実装（設計に届いていない）

### 2-1. ✅ 想定ロックオン（センサー推定射撃）— 実装完了
- **ダメージデバフ（#39取込→#40）**: 完全ロック(視野内`inVision`)=フルダメージ / 想定ロック(センサーのみ)=
  `contactAccuracy`依存のダメージデバフ(30〜80%)。`Projectile(...,dmgScale)`引数を追加し全武器に適用。ロック種別変化時にログ表示
- **beam長射程狙撃（本ブランチ）**: 想定ロック(`_assumedLock`)時、beamの射程ゲートを8000へ延長＝**ヒッグスダークチャネルでマップ端から狙撃可**（設計 `MEMORY:84-88`）@`game.js:1316-1325`
- **命中ジッター（本ブランチ）**: 想定ロックのbeamは精度が低いほど外れる（`miss率=(1-acc)*0.6`、外れ時はジッター点へ無害ビーム＝発射で自位置だけ露出）@`game.js:1410-1430`
- **レティクル色分け（本ブランチ）**: 完全ロック=緑実線ブラケット / 想定ロック=琥珀破線ブラケットを現ターゲットに描画 @`game.js:2382付近`
- ✅ **kinetic/missile想定ロックジッター（PR#89）**: kinetic=発射角に`(rand-0.5)*(1-acc)*0.55`rad散弾（assault3連装も同方向ジッター）/ missile=ダミー座標`(1-acc)*500u`オフセットへ誘導。ログ出力あり @`game.js:1910付近`

### 2-2. ✅ センサー制約型敵AI（予測モデル+適応戦略 実装完了）
- **予測照準（PR#35）**: `playerLastKnownPos{x,y,vx,vy}` / `contactFreshness`（喪失で減衰）。combat時は**最終既知位置へ予測照準**（鮮度に応じ速度リード外挿、接触喪失で凍結）。ビームは照準点が実機に近い時のみ着弾＝沈黙/ステルスで外れる＝プレイヤーと**対称**
- **適応戦略テーブル `predictedBehavior`（本ブランチ）**: 全知禁止のまま**検知した支配的シグネチャ**から行動を推定→適応 @`game.js:1556-1620,1640-1700`
  - EMスパイク+ノード近接 → `gathering` 推定 → **ノードへ先回り**（huntTargetをノードに）
  - 熱+EM強・光学弱 → `charging`（ビームチャージ）推定 → **側方ストレイフ回避**
  - 光学支配 → `kinetic`（実弾近接）推定 → **距離を取りカイト＋武器を長射程へ切替**
  - 無反応 → `silent`（潜伏）推定。`confidenceLevel`は`contactFreshness`で代替
  - 推定が変わると敵AIログに表示（プレイヤーへのフィードバック）
- **残（✅完了 2026-06-20）**: `silent`時のヒッグス雲集中捜索バイアス実装。lurking状態で`predictedBehavior==='silent'`のとき確率0.4%→1.4%に増加し、探索中心を`playerLastKnownPos`（鮮度>5%時）、探索半径を2500→4500に拡大して`findHidingSpot`で最密域へ向かわせる（`game.js:2417-2423`）

### 2-3. ✅ ソナーエフェクト改善（残像実装済・PR#35系）
- **実装済み**: 全方位/指向性ソナー + **`sonar-fill`残像エフェクト**（最大半径到達でフェードアウト, `life:360`≒6秒）@main`game.js:2474-2486,3555`
- **残（任意）**: 派手な色彩・波形伝播の更なる演出強化は好みに応じて

---

## 3. ❌ 未実装（壁打ちで企画・要実装）

### 3-1. ✅ アップグレードツリー再整合（PR#53）

| 種別 | 新効果 |
|---|---|
| エンジン Lv1/2/3 | ヒッグス/デブリ減速軽減 20/35/50% + heatSig低下 10/20/30% |
| 武装 Lv1/2/3 | 射程×1.15/1.30/1.50 + リロード×0.85/0.70/0.55 |
| 装甲 Lv1/2/3 | kinetic耐性25% → +missile25% → +beam25%（累積） |
| センサー Lv1/2/3 | ソナー範囲×UPGRADE_MULT（変更なし） |

- **定数**: `ENGINE_UPG_HIGGS_RESIST`, `ENGINE_UPG_HEAT_REDUCE`, `WEAPONS_UPG_RANGE_MULT`, `WEAPONS_UPG_RELOAD_MULT`, `ARMOR_RES_KINETIC/MISSILE/BEAM`
- **残（任意）**: センサー精度/レンジ分離・ドローン生産アンロックは将来拡張へ

### 3-2. ✅ AI 並列精度制御（独立スライダー）— 実装済
- **設計**（`game_design_v2.md` §5-2）: GEN配分とは別に、AI出力(`genAlloc.ai`)を解析/命中/回避にゼロサム配分。
- **実装**（`game.js` / `index.html`）: `aiPrecision={sensor,weapon,engine}`（ゼロサム3スライダー、GENと同方式UI）。`aiPrec(key)=(genAlloc.ai/100)*(aiPrecision[key]/100)`。
  - **解析(sensor)** → `applyContact`でコンタクト精度を `×(1+aiPrec*AI_SENSOR_ACC)` ブースト（候補§3-3も自動的に絞れる）
  - **命中(weapon)** → 自機弾のデブリミス率を `×(1-aiPrec*AI_WEAPON_AIM)` 低減
  - **回避(engine)** → 敵弾の自機被弾を `aiPrec*AI_ENGINE_DODGE` 確率で回避（"回避"フロートテキスト）
  - **トレードオフ**: AIを上げるほど精度↑だが `playerEmBoost=0.5+ai/100*0.5` でEM放射↑＝逆探知されやすい（情報↔露出、既存§1法則と整合）
- **残（任意）**: ジャミング/デコイ解析耐性への明示反映（現状は精度ブースト経由で間接的）。数値(AI_*)は実機調整用tunable。

### 3-3. ✅ AIロックオンシステム（仮想ターゲット候補）— 実装済（本ブランチ）
- **設計**（`MEMORY:217-221`）: センサー検知 → AI解析 → **仮想ターゲット候補を確率%付きでマップ表示**
- **実装**（`game.js`）: `makeContactCandidates(acc)` がコンタクト精度から候補群 `{dx,dy,p}` を生成。
  `contactAccuracy < 0.7` の非視野センサーコンタクトに対し、推定中心(`displayX/Y`)から候補ダイヤモンドを%付きで描画。
  - 候補数 = `2 + (1-acc)*4`（高精度=2〜3個 / 低精度=5〜6個）、分散 = `(1-acc)*360`。
  - 本命(index0)が支配的確率（高精度ほど偏り、低精度ほど均等）＝高信頼度は本命が明確/低信頼度は迷う。
  - `_candAcc` で精度変化時のみ再生成（マーカーが毎フレーム揺れない）。完全ロック(視野内)では非表示。
- **残（任意）**: 信頼度を §3-2 AI解析精度スライダーに連動（現状は `contactAccuracy` を信頼度に流用）。デコイ/ドローンの偽候補常時混入は §3-5/§3-6 実装時に統合。

### 3-4. ✅ 潜航型ジャミング3種（実装完了・PR#41）
- **実装**（stealth専用、`game.js` 上部 `jamBurst/jamCont/jamPulse` + 敵探知 `myDetectRange*(1-jamDegrade)`）:
  1. **範囲ジャミング (JAM)** — 半径3500内の敵探知を60%劣化・6秒（`JAM_BURST_*`）
  2. **継続EMジャム (EM-J)** — トグルON中、半径2400内を35%劣化（`JAM_CONT_*`）
  3. **EMパルス (PULSE)** — 半径6000を1.25秒**全ブラインド**、CD20秒（`JAM_PULSE_*`）＋パルス演出
- **トレードオフ**: 発動中は自`emSig`増（burst+0.4 / cont+0.25 / pulse=最大）＝**EM逆探知されやすい**（情報↔露出）
- **UI**: 潜航型選択時のみ表示する3ボタン（`startGame`で`.jam-btn`表示制御、紫系）
- **数値はバランス調整用デフォルト**（半径・劣化率・CDは実機調整前提のtunable定数）
- **残**: ジャミング範囲の可視リング表示（現状はパルスのみ演出）は任意で追加可

### 3-5. ✅ 潜航型デコイ射出（実装完了・PR#41）
- **実装**（stealth専用、`decoys[]` + `updateDecoys()`/`drawDecoys()`）: 射出方向へ慣性で飛ぶデコイ実体。寿命8秒（`DECOY_LIFE`）
- **ミサイル誘引**: 敵ミサイルは`update()`で半径`DECOY_LURE_RADIUS=1600`内のデコイを本標的より優先追尾、到達で無害化（ミサイル消費＋デコイ消耗）@`game.js Projectile.update`
- **同時上限**: 積載量 `CARGO_CAP.stealth=3`（§3-8）
- **UI**: 潜航型選択時のみ表示の DECOY ボタン（`.jam-btn`）
- **残（✅完了）**: デコイの強EMで敵のlastKnownPosをデコイ位置へ書き換え誤誘導 (`DECOY_MISDIRECT_RADIUS=2500`、30f毎チェック、contactFreshness<0.5の敵のみ対象)。誤誘引成功時に「DECOY: 敵AIが誤誘引」ログを1回出力 (`game.js updateDecoys()`)。`decoyActive`(難破船ハック偽コンタクト)とは別系統

### 3-6. ✅ 空母型ドローン4種（自機側）— 実装済
- **実装**（`Drone`クラス + `playerDrones[]` + `deployDrone()`/`updatePlayerDrones()`/`drawPlayerDrones()`、carrier専用4ボタン `.drone-btn`）:

  | ドローン | 実装挙動 | tag |
  |---|---|---|
  | 攻撃(attack) | `DRONE_ATK_RANGE`内の可視敵を自動追尾しkinetic射撃(dmg×0.5)。敵不在時は自機護衛 | ATK |
  | デコイ(decoy) | 射出慣性で飛び強EM波紋。**敵ミサイルを誘引**(missile homingに`playerDrones`のdecoyを追加) | DCY |
  | 哨戒(scout) | 自機前方へ進出＆周回。`DRONE_SCOUT_RANGE`内の敵に`applyContact`=センサーの目耳を前進配置 | SCT |
  | 建設(build) | 固定タレット。`DRONE_TURRET_RANGE`内の敵へ自動射撃(dmg×0.6)。§3-7の最小実装 | TUR |

- **同時展開上限**: `CARGO_CAP.carrier=6`（§3-8）。`DRONE_LIFE=2400`(40秒)で帰投。
- **残（✅完了 2026-06-20）**:
  - ✅ ドローン被弾/撃墜: 敵弾が`playerDrones`に当たるとHP減少 (`Projectile.update()` 末尾, dmg×0.8), HP≤0で撃墜ログ
  - ✅ 攻撃ドローン熱シグネチャ: `contactFreshness<0.3`の敵がATKドローンを感知→`playerLastKnownPos`を書き換え (误诱引, `game.js`)
  - ✅ 建設中停止フロー: `buildingTimer=BUILD_STOP_DUR(180f=3秒)` で完全停止+全シグネチャ増大(×2.0)。build/barrier/buoy/higgs展開時に発動 (`deployDrone()`)

### 3-7. ✅ 空母型建設物4種（§3-6タレット含む全4種実装完了・PR#51）

| 建設物 | ボタン | 機能 | シグネチャ |
|---|---|---|---|
| 砲台 | D-BLD | kinetic自動射撃(dmg×0.6)。射程1100 | 熱 |
| ビームバリア | D-BAR | 周囲880px内の敵に周期ダメージ(10/hit)。寿命2分 | EM弱 |
| センサーブイ | D-BUY | 半径3200px広域スキャン→コンタクト付与(2.5秒周期) | EM放射=位置バレ |
| ヒッグス散布装置 | D-HGS | 局所ヒッグスウェイク連続生成→濃度増大→盾/ビーム無効化 | 不自然な雲 |

- 建設物寿命: `DRONE_BUILDING_LIFE=7200`(2分)。展開上限は`CARGO_CAP.carrier=6`に計上
- **残**: スプライト未生成。建設停止フローは §3-6 実装分 (`buildingTimer`) で対応済み

### 3-8. ✅ 積載量（cargo capacity）— 数値オーナー合意済み
- **用途**: ドローン/デコイの最大同時展開数を制御
- **✅ 確定数値（2026-06-14 オーナー決定）**: **空母型 6 / 潜航型 3 / 攻撃型 2**（同時展開上限）
  - tunable定数として実装し、艦種別ドローン/デコイ展開のキャップに使用
- **適用先**: デコイ射出（§3-5, 潜航=3）/ 空母ドローン（§3-6, 空母=6）

### 3-9. ✅ 修復ドローン（完全停止アップグレード・PR#51）
- **実装**: `repairActive`フラグ + REGENボタン（全艦種）。ON時: 完全停止 + HP回復(1.2/frame) + 全シグネチャ増大(×2.4) + 緑パルスリング描画。修復完了で自動OFF。
- **トレードオフ**: 停止中は全センサーに最大脆弱（設計通り）
- **残（✅完了 2026-06-20）**: 艦種別の検知性差異を実装。assault=[heat:0.38,em:0.28,opt:0.22,hig:0.10]（溶接→高熱+光学）/ stealth=[0.15,0.55,0.08,0.07]（電子修復→EM急騰）/ carrier=[0.22,0.42,0.14,0.14]（複合システム→EM+ヒッグス）。`game.js:1815-1819`

### 3-10. ✅ ミサイル2タイプ（PR#51）
- **実装**: `missileMode='homing'|'smart'`。MSL:HON/AIボタン（全艦種）でトグル。
  - **HON（熱源誘導）**: 速度6/射程1500/dmg50。デコイ/ジャミング妨害可（従来通り）
  - **AI（追跡型）**: 速度7.5/射程2200/dmg65。デコイ耐性(`aiPrec*0.9`確率で無視)。着弾時大閃光（光学強・位置特定される）
- **残（✅完了 2026-06-20）**: ジャミング（JAM_BURST/CONT/PULSE）が敵ミサイル誘導に干渉するよう実装。旋回精度 ×0.40/×0.65/×0.02(パルス=ほぼ無効化)。`Projectile.update()` の `_missileTurn` 計算に `_jamMissileFactor` を乗算

### 3-11. ✅ エンジン噴射トレイル色（PR#36で実装済）
- **実装済み**: `gameState.engineType`に応じてトレイルのグラデ色を切替（`ENGINE_THRUST`/`drawThruster`）→ §1「艦種・エンジン・描画」参照。当項目は完了

### 3-12. ✅ パッシブ方位ウェッジ（"索敵が運ゲー"問題の解・PR#35で実装済）
- **実装済み**: §1「索敵・センサー」参照。パッシブ探知に方位ウェッジ（強信号=狭い確信/弱信号=広いボケ）、計測位置アンカー、移動2回計測で三角測量。敵AIも`playerLastKnownPos`で**対称化**（§2-2）。「運」→「推理」の核は実装完了
- **残る付随施策**:
  - ✅ シグネチャを**減衰するtrail**として全センサー種へ一般化（`heatTrails`/`opticTrails`/`emTrails`）→「足跡を追う」体験。PR#52で実装済
  - 「隠れる」の**撹乱化**: ✅ デコイ偽方位/lastKnownPos誤誘導 (§3-5実装済) / ✅ ジャミングで相手の方位を広げる — JAM_BURST中は±400のランダムノイズ、JAM_CONT中は±250のノイズを`playerLastKnownPos`に加算し方位精度を劣化（`game.js:2185〜`）。難破船ハック偽コンタクトは実装済。静止無敵の不可能化（EM自然漏洩＋ヒッグス自然成長で接敵強制）

### 3-13. 🚧 マップ4層構造 + 地形シグネチャ要素（ビジュアル改良・2026-06-14壁打ち）
> **進捗**: Phase1（B:雲見上げ表現+%表示 / C:離れた濃いポイントの視界バランス）= ✅実装済（本ブランチ）。Phase2（デブリ帯+磁気嵐帯）= ❌未着手。
> **目的**: ヒッグスのビジュアルを「空に浮かぶ雲を下から見上げた」表現に格上げし、
> ヒッグス以外にも**センサー別の地形ハザード**を導入して「動く・索敵する・隠れる」の駆け引きを地形でも成立させる。
> オーナー案＋本セッションの設計提案。**数値・4種目地形・バランスは未確定（要オーナー合意）**。

#### A. マップ4層構造（描画レイヤー）— ✅ アーキテクチャ確定
1. **背景層（最奥）**: 星・星雲パララックス（`generateSpaceBackground`/`_drawNebula`）。実装済
2. **メインフィールド層**: 自機・敵・ノード・構造物（エンティティ）
3. **地形層（局所ハザード）**: デブリ帯 / 磁気嵐帯 / 熱雲（+4種目）。**それぞれ濃度マップを持つ**（`bgMist`同様のブロブ集合）
4. **ヒッグス層（最前・最重要）**: 全センサーに干渉するゲームの根幹。雲を下から見上げた濃淡表現

#### B. ヒッグスのビジュアル改良（雲を見上げる表現）— ✅ 実装済（Phase1・本ブランチ）
- **旧課題**: `drawFogOfWar`は自機周囲を**アメーバ穴で一様にクリア**し、視野内は濃度勾配を表現していなかった。
- **実装**:
  1. ✅ 白いヒッグス雲を事前焼き付け（`higgsCloudCanvas`、`generateSector`内・768px、濃度連動の白さ`coreA=0.05+d*0.75`）→ `drawFogOfWar`で**視野アメーバにクリップし `'lighter'` 合成**（@`game.js` drawFogOfWar末尾）。濃密部=ほぼ真っ白／疎部=青空が透ける。自機濃度が低く視界が広くても視界内の濃い箇所は曇る。
  2. ✅ 自機地点の濃度の%表示は**既存実装**（`env-higgs`、`updateEnvInfo()` @`game.js:4368`、`Math.round(intensity*100)+'%'`）。
- **残（任意）**: Eの統合地形パネルへ他地形密度と並べる（Phase2以降）。

#### C. 局所視界と「離れた濃いポイント」のバランス — ✅ 実装済（Phase1・本ブランチ）
- **オーナー懸念**: 視界半径を**自機地点のヒッグス濃度のみ**で決めると、自機が薄い所＝視野広 → 遠くの濃い所に隠れた敵まで見えてしまい、「ヒッグス濃度の影響を受ける主体が自機だけ」になる。
- **実装モデル（target-local ゲート）**: 敵の視認可否を判定（@`game.js` `updateVisionLockOn`）。
  1. **自機ローカル濃度** → 視野リーチ（既存 `computeVisionRadius`、自機位置のみ）
  2. ✅ **ターゲットローカル濃度（新規）** → 敵地点の濃度から`clarity = 1 - clamp((hTarget-0.22)/0.55)`を算出。`clarity>=0.85`=完全ロック / それ未満は`inVision=false`で**想定ロック止まり**にし、`contactAccuracy`を`0.45..0.87`（濃密潜伏は0.25）に**直接キャップ**（applyContactは精度を下げないため）。定数`HIGGS_CLEAR_BELOW=0.22`/`HIGGS_CLEAR_SPAN=0.55`はtunable。
  3. **視線経路の濃度（一部既存）** → ビームは`higgsBetween`で経路濃度を既に参照。視認/狙撃の経路減衰への一般化は残課題。
- **狙い実現**: 「すぐ先の雲の中に何か居る、でもクリーンにロックできない」。既存の**完全ロック/想定ロック2層（§2-1）を再利用**（`_fullLock=!!inVision`→武器側で自動的に想定ロック扱い＝ダメージデバフ・beamのみ長射程ジッター）。
- **対称性**: 敵AIは元々センサー制約型＋ヒッグス経路減衰で自機を検知（`higgsPath`）。自機が濃いポケットに居れば敵の検知も減衰＝対称は概ね担保。完全な「敵側の視認ゲート」は将来拡張。
- **下限**: 絶対不可視にはしない（`clarity>0.1`未満でも微弱コンタクト維持。ビーム・アクティブソナーは貫通、ダークチャネル長射程狙撃 §2-1 と整合）。

#### D. 地形シグネチャ要素（センサー別ハザード）— 🚧 2種実装済（Phase2・本ブランチ） / 熱雲・4種目は保留
- **✅ Phase2 実装（デブリ帯+磁気嵐帯）@`game.js`**:
  - フィールド: `debrisField[]`(10ブロブ) / `stormField[]`(6ブロブ)。`getDebrisIntensity`/`getStormIntensity`（フレームキャッシュ+500量子化）。ベイク: `debrisCanvas`(岩片点描・灰) / `stormCanvas`(紫青EMノイズ・描画時明滅)。`drawBackground`で bgMist の後に描画。
  - **デブリ効果**: 移動減速（`terrainSpeedMult()`・自機/敵共通、自機はAI配分で最大70%軽減＝`DEBRIS_AI_MITIGATE`）／実弾・ミサイルのミス率↑（`DEBRIS_MISS`、ビームは即着弾でこの判定を通らず=貫通）／敵の光学探知を経路減衰（`DEBRIS_OPTIC_MOD`）。
  - **磁気嵐効果**: 嵐内の機体のEMシグネチャ低減（`STORM_EM_MASK`＝AIを安全に回せる退避所）／敵のEM探知を経路減衰（`STORM_EM_MOD`）。
  - UI: 左パネルに `岩礁:xx% / EM嵐:xx%` 追加（`env-debris`/`env-storm`、`updateEnvInfo`）。
  - 全数値は tunable 定数（`DEBRIS_SLOW/AI_MITIGATE/MISS/OPTIC_MOD`, `STORM_EM_MOD/MASK`）。**実機30fps計測が必要**。
- **✅ 追加実装（本ブランチ・Phase2仕上げ）**: 敵側のデブリAI軽減（固定`DEBRIS_ENEMY_MITIGATE=0.35`で全停止を防止）／磁気嵐内のミサイル誘導劣化（旋回精度↓ `STORM_MISSILE_DEGRADE`）／ミニマップにデブリ(灰)・磁気嵐(紫)オーバーレイ追加（退避先把握用）。
- **✅ Phase3 実装（熱雲・PR#55）**: `thermalField[]`(7ブロブ) + `getThermalIntensity()` + `thermalCanvas`(赤橙ベイク768px)。`THERMAL_HEAT_MASK=0.60`で熱雲内heatSig低減・`THERMAL_HEAT_MOD=0.80`で敵HEAT探知経路減衰。ミニマップ赤橙オーバーレイ + env-thermal/msb-thermal UI。
- **残（任意）**: 4種目地形、アクティブソナーの嵐劣化（ミサイルは実装済）。
- **設計原則**: ヒッグス=**全センサー干渉**（根幹）。それ以外の地形は**1センサー軸に対応**させ、「その signature を隠す隠れ場所」を地形で作る。
- **提案マトリクス**（オーナー案を1:1対応へ整理。デブリの熱+光学を**デブリ→光学 / 熱雲→熱に分離**するのが本提案。理由は後述）:

  | 地形 | 主干渉センサー | 移動/戦闘への副作用 | フレーバー |
  |---|---|---|---|
  | **ヒッグスの霧** | 全4種 | エンジン減速・ビーム減衰（実装済） | 根幹。雲を見上げる濃淡 |
  | **デブリ帯/岩礁帯** | OPTIC（遮蔽） | **移動速度↓**（すり抜け）・**敵の実弾/ミサイル命中率↓**（遮蔽）・ビームは貫通で無効 | 物理障害物 |
  | **熱雲/プラズマ雲** | HEAT（環境熱ノイズ） | 自機の熱シグネチャが埋もれる（隠れやすい）／滞在で熱漏れ増の可能性 | 高温ガスがエンジン熱を覆い隠す |
  | **磁気嵐帯** | EM | EM探知を撹乱・**ミサイル誘導/アクティブソナー劣化**・AI配分を上げてもEM露出しにくい（嵐がEMを覆う） | 電磁干渉 |

- **デブリのバフ・デバフ詳細（オーナー案）**:
  - 滞在機体の**移動速度デバフ**（障害物すり抜け）。**AI演算配分↑で姿勢制御補助＝デバフ軽減**（敵から逃げる際にわざと岩礁帯へ逃げ込む戦術）。
  - デブリ帯内の機体への**実弾・ミサイル命中率デバフ**（遮蔽）。**ビームは高熱で貫通＝デバフ無効**。
  - OPTICセンサーへのデバフ（障害物に紛れて隠れる）。
- **創発シナジー（本提案の推し）**: デブリ帯=ビーム有利／ヒッグス帯=実弾・ミサイル有利、と地形間でじゃんけんが成立する。磁気嵐帯=AIを安全に増やせる退避所（EM∝AI法則の例外地帯）。
- **4種目について**: 上表のように HEAT/OPTIC/EM を各1地形で覆い、HIGGSは霧（根幹）が担う、で**4センサー全網羅**。オーナー案の「デブリ=熱+光学」を維持する場合は4種目が宙に浮く（HIGGSは根幹で重複）ため、**分離案を推奨**。
- **✅ オーナー決定（2026-06-14）**: **まず2種（デブリ帯=OPTIC / 磁気嵐帯=EM）だけ実装 → 30fps維持を実機計測してから熱雲/4種目を判断**。段階導入でパフォーマンスリスクを抑える。
  - 熱雲（HEAT）の追加可否・デブリの熱+光学維持か光学専任かは**2種実装後に再決定**（4種目=重力レンズ帯案も保留）。
- **要オーナー決定（残）**:
  - (b) 各地形の密度レンジ・速度デバフ%・命中率デバフ%・AI軽減カーブ（全てtunable定数で実装し実機調整前提）。
  - (c) 地形の出現量。デブリは「程々」（スピード感を損なわない密度＝ヒッグスより薄く散在）でオーナー合意済みの方向。

- **段階実装プラン**:
  - **Phase 1**: ✅ B（ヒッグス雲見上げ表現+%表示）+ C（離れた濃いポイントの視界バランス＝想定ロックゲート）。本ブランチで実装済。
  - **Phase 2**: ✅ デブリ帯（OPTIC・移動/命中デバフ・AI軽減・ビーム貫通）+ 磁気嵐帯（EM撹乱・AIをEM露出なく回せる退避所）。本ブランチで実装済。**実機30fps計測が未実施**。
  - **Phase 3**: ✅ 熱雲（HEAT）実装済（PR#55）。4種目は任意拡張。

#### E. 用語・UI提案
- **デブリ密度の呼称**: HUD表示は簡潔に **`DEBRIS`（デブリ密度）** を推奨（「周辺デブリ密度」はHUDには冗長）。世界観フレーバー名は **岩礁帯**（潜水艦モチーフと整合）。
- **統合地形パネル**: 自機地点の各地形密度を%で並べる小パネル（例: `HIGGS 72% / DEBRIS 18% / EM-STORM 0% / THERMAL 5%`）。アクティブな地形のみ表示。
- **濃度連動の演出**: ヒッグス=白〜青の雲、デブリ=点描の岩片、磁気嵐=明滅するEMノイズ、熱雲=赤橙のゆらぎ、で4地形を色で識別。

#### F. 実装ガードレール（パフォーマンス）
- 地形密度関数は `getHiggsIntensity` と同様に**フレームキャッシュ＋グリッド量子化**必須（毎フレーム・全エンティティで複数回呼ばれる）。
- 地形ブロブは**事前焼き付けキャンバス（≤1024px）**にベイク（`bgMistCanvas`方式）。`shadowBlur`禁止・動的色文字列禁止（HANDOVERパフォーマンス教訓）。
- 地形種が増えるほどコスト増 → ブロブ数は控えめ＋LOD（画面外・遠景は密度ルックアップ省略）。

---

## 4. 🧊 構想段階（仕様未確定・将来）

### 4-1. 🧊 艦級×艦種マトリクス（2026-06-20 壁打ち確定）

**設計思想**: Uボートが戦艦を沈めたように、**小型艦でも立ち回り次第で大型艦に勝てる**。サイズの非対称戦が核心。センサー4種じゃんけん・ヒッグス雲・後方被弾ボーナスが既にそれを支える構造になっている。

#### 艦級×艦種マトリクス

| 艦級 | 規模感 | 攻撃型 | 潜航型 | 輸送/空母型 |
|---|---|---|---|---|
| **Fighter**    | 超小型・使い捨て | ✓ | — | — |
| **Corvette**   | 小型高機動 | ✓ | — | — |
| **Frigate**    | 中型汎用 | ✓ | ✓ | — |
| **Destroyer**  | 大型多用途 | ✓ | ✓ | ✓（輸送艦 = 空母下位） |
| **Battleship** | 超大型主力 | ✓ | ✓ | ✓（大型輸送艦） |
| **Mothership** | 最大・司令艦 | ✓ | — | ✓（正規空母） |

#### 艦種の意味（縦軸）
- **攻撃型**: 火力・耐久重視。シグネチャ大。正面突破
- **潜航型**: 高速・ステルス・ジャミング。シグネチャ極小。失敗即死
- **輸送/空母型**: ドローン・建設物・支援。停止中=最大脆弱。Destroyer以上のサイズが必要

#### 現行コードとの関係
- 現在の `corvette/destroyer/carrier/fighter` は**すべて敵ユニット型**（`ENEMY_TURN_RATES` 等）。自機は艦種（assault/stealth/carrier）で管理。将来的には統一した`shipClass`×`shipRole`の2軸に整理する想定
- `gameState.enemyType: 'assault'|'stealth'|'carrier'` → 敵の艦種選択は実装済。艦級スケールは未実装

#### 艦級別スペック骨格（2026-06-20 確定方針）

| 艦級 | 速度 | 耐久 | 扱える武器 | ドローン |
|---|---|---|---|---|
| **Fighter**    | ★★★★★ | ☆ | 軽量Kinetic | — |
| **Corvette**   | ★★★★ | ★ | Kinetic / 軽Missile | — |
| **Frigate**    | ★★★ | ★★★ | Kinetic / Missile / 軽Beam | — |
| **Destroyer**  | ★★ | ★★★★ | Missile / Beam / 重Missile | Fighterサイズ |
| **Battleship** | ★ | ★★★★★ | 全種・最大威力 / 多門同時発射 | Fighter/Corvette |
| **Mothership** | ☆ | ∞ | 艦隊規模武器 | Fighter/Corvette |

**設計法則**:
- **速度**: 艦級が小さいほど速い（Fighter ≈ 現stealth×1.5 / Mothership ≈ 現carrier×0.5 想定）
- **耐久**: 艦級が大きいほど高HP（Fighter ≈ 現stealth程度 / Mothership ≈ 現carrier×5+ 想定）
- **武器**: 大型艦ほど威力・射程が高い武器を装備可。現行3種（kinetic/missile/beam）を「軽→重」でティア展開
- **ドローン**: Destroyer以上の輸送/空母型が展開可。ドローン本体サイズはFighterまたはCorvetteクラス（それ以上の大型ドローンは存在しない）

#### 未確定（要オーナー決定）
- 各艦級の具体数値（速度倍率・HP値・旋回率・積載量）
- 武器ティアの詳細（「重Missile」「軽Beam」等の数値差分）
- セクター進行と敵艦級スケールの対応（序盤=Fighter/Corvette、終盤=Battleship/Mothership）
- 自機の艦級アンロック条件（SCR購入 / セクタークリア など）
- Mothershipはプレイヤーが操作可能（最終艦級。敵専用ではない）✅確定

### 4-2. 🧊 自機乗り換え・カスタマイズショップ
- **構想**（`MEMORY:192-195`）: SCRで自機乗り換え・装備購入。各クラスのカスタマイズで戦略特化。将来のショップ/カスタマイズメニュー

### 4-3. 🧊 アートワーク方針
- **方針**（`MEMORY:197-213`）: 世界観は**戦艦同士の戦い**（ファイター＝ドローン扱い）。各クラスのプロファイル/トップダウン両アセットが必要
- **確定アセット**: 攻撃型バトルシップのサイドビュー1枚（重厚・多面体装甲・連装砲塔・ミサイルポッド・シアン炎スラスター）→ トップダウン加工予定
- **生成パイプライン詳細**: `HANDOVER.md` のアセット生成手順を参照

### 4-4. 🚧 シグネチャ解析 → 座標射撃システム（2026-06-20 壁打ち確定）

> **実装進捗**:
> - ✅ Phase1 (PR#86): `passiveBearings[]` 拡張 + `computeTriangulation()` + `drawTriangulationCircle()` + 解析パネル (sig-canvas タップ)
> - ✅ Phase2 (PR#87): ゴースト表示 (精度≥80%: 菱形シルエット+"?") + `coordFireAtTriangulation()` + COORD FIRE ボタン
> - 🚧 Phase3: ✅ ゴースト速度外挿 (`_prevTrigResult`→`triangulationVelocity{vx,vy}`、ゴーストが外挿位置へ移動+向き方向表示+GHOST v0.0/sラベル) / ✅ 命中率の武器飛翔時間考慮 (ミサイル: 飛翔時間×推定速度=ドリフト量を命中率に反映、ビーム: データ経過時間で精度減衰) / ❌ 解析モーダルを開いている間の移動ペナルティ（要オーナー決定）

> **ゲーム性の核**。センサーで受信したシグネチャを解析・三角測量し、発信源の推定座標に向けて「賭けの射撃」を行うループ。「見えたら撃つ」ではなく「情報戦で確率を上げて撃つ」が本質。

#### 設計思想・3つの原則
1. **移動の方向がデータの質を決める**: 受信ポイントAから正しい方向（三角測量に有効な側面方向）へ移動すると方位線の交差角が大きくなり精度が上がる。頓珍漢な方向（方位線と平行）に移動すると新情報がゼロ。移動の方向選択自体がゲームプレイ。
2. **精度2層分離**: 「位置特定精度（どこにいるかの信頼度）」と「命中率（弾が当たるか）」は別物。95%精度でも命中率は低くなりうる。
3. **解析が敵に逆用される**: 自機がシグネチャを出す瞬間は敵に位置を特定されるリスク。「敢えて動いて情報を出し相手の三角測量を誤らせる」が自機側の戦術になる。

---

#### A. シグネチャ受信ログ

パッシブ検知ごとに以下を記録:
```
{
  sensor: 'heat'|'optic'|'em'|'higgs',
  strength: 0.0〜1.0,          // 信号強度
  bearing_min: 0〜360,         // 方向範囲の下限
  bearing_max: 0〜360,         // 方向範囲の上限（差=不確実性）
  position: {x, y},            // 受信時の自機座標
  timestamp: frame,            // 受信フレーム
  identified: null|string      // 解析後のシグネチャ種別
}
```

**角度範囲の幅（bearing_max - bearing_min）の決定要因**:
- 信号強度が強い → 幅が狭い（方向が明確）
- 信号強度が弱い → 幅が広い（ぼんやりした方向感）
- ヒッグス密度が高い経路 → 幅を追加拡張（散乱）

---

#### B. 解析モーダル（オシロスコープをタップして開く）

```
┌─ SIGNAL ANALYSIS ──────────────────────────────────┐
│ [オシロスコープ波形]                                 │
├────────────────────────────────────────────────────┤
│ センサー  強度         方向              解析状態   │
│ HEAT    ██████░░ 78%  047°〜062° (15°)  [解析する] │
│ EM      ██░░░░░░ 24%  110°〜195° (85°)  [解析する] │
│ HIGGS   ████░░░░ 51%  288°〜301° (13°)  [解析する] │
└────────────────────────────────────────────────────┘
```

- **角度差が小さい = 明確な方向**（例: 15° → 信頼できる方位線）
- **角度差が大きい = 曖昧な方向**（例: 85° → 三角測量に不向き）
- マップ外縁に360°目盛りを表示。リストの方向と連動
- 解析中も移動可能（良い方向に動くと解析精度が上昇）

**シグネチャ種別の特定精度**（AIゲイン × 解析スライダー係数で決まる）:
- 係数高・信号強度強 → 「敵のビームチャージ」まで特定できる
- 係数低・信号弱 → 「何らかのEM反応」程度しかわからない
- 係数低 → **誤判定のリスク**（EM嵐をミサイル熱源と判定するなど）。誤情報を元に三角測量すると解析結果が根本から外れる

---

#### C. 三角測量エンジン（内部計算）

```
精度スコア = f(
  ベースライン長さ × sin(方位角変化量),  ← 移動の質（垂直方向=最大）
  交差方位線の本数,                       ← 多いほど精度UP
  データの新鮮度（古いほど劣化）,
  各方位線の角度差の逆数（狭い線が有利）,
  経路上のヒッグス濃度（上限キャップ）    ← 最大精度を抑制
)
```

**ヒッグスによる精度上限**:
- ヒッグス高密度エリア: 上限 20〜30%（どれだけ頑張っても博打止まり）
- クリアなエリア: 上限 70〜80%（完璧なデータ収集で高精度）
- ゲーム内の理由: ヒッグス場が信号を散乱・屈折させるため

**センサー種別ごとの「良い移動方向」**:
| センサー | 精度が上がる移動 | 理由 |
|---|---|---|
| HEAT | 発信源に接近 | 距離で信号強度変化→距離推定 |
| OPTIC | 遮蔽なしの側面 | 光学は直線、射線確保が必要 |
| EM | 方位線への垂直移動 | 長距離・広角向き、三角測量に最適 |
| HIGGS | ウェイク軌跡を横断 | 速度ベクトルを読める |

---

#### D. 解析結果の表示

**精度円（レーダー・マップモードに表示）**:
- 精度低（〜30%）: 大きな赤い円
- 精度中（30〜60%）: 橙の中円
- 精度高（60〜80%）: 緑の小円
- 精度 95%超: 円が消え**ゴースト表示**に移行

**ゴースト表示（95%以上）**:
- レーダー・マップモード・フィールド上に半透明の敵シルエット
- 解析時点の位置＋速度ベクトルから**外挿で動く**（針路変更されると徐々にズレていく）
- あくまで推定。デコイを解析した場合はデコイのゴーストが出る（本体は別場所にいる）
- 視覚的に「?」マークや透過度で「確定ではない」ことを明示

**精度の経時劣化**:
```
解析完了直後: 91%（サークル小）
30秒後:      78%（少し広がる）
2分後:       45%（かなり広い）
5分後:       20%（ほぼ無意味）
```
敵が針路変更するとさらに急激に劣化（外挿が外れていく）

---

#### E. 射撃決断UI（長押しラジアルメニュー → 予測座標を選択時）

現行の長押し＝ウェイポイント設定をラジアルメニューに拡張:
```
長押し → [ここへ移動 / この座標に射撃 / キャンセル]
```

「この座標に射撃」を選んだ場合:
```
┌────────────────────────┐
│  解析精度   ████████░ 91%  │  ← 位置の信頼度（どこにいるか）
│  推定命中率 ████░░░░░ 43%  │  ← 弾が当たる確率（別計算）
│                         │
│  [ 発射 ]  [ キャンセル ] │
└────────────────────────┘
```

推定命中率の計算要素:
- 精度スコア（位置の信頼度）
- 武器の飛翔時間（missile: 到達前に敵が動く）
- 射線上のヒッグス濃度（beam散乱）
- 解析完了からの経過時間
- 敵の推定移動速度

**武器種別の座標射撃挙動**:
| 武器 | 座標射撃の挙動 |
|---|---|
| Kinetic | 精度円の範囲に散弾パターン（面でカバー） |
| Missile | 座標へ飛行後、到達時に周辺を短時間ターミナルスキャン |
| Beam | ピンポイントのみ（精度低 = 完全外れ・最高リスク）|

**外した時のペナルティ**: 発射自体で自機シグネチャが露出（kinetic=光学 / missile=熱 / beam=ヒッグスダークチャネル+EM）。撃つ行為が自分の位置を教える。

---

#### F. 既存コードとの接続ポイント

| 新機能 | 使える既存コード |
|---|---|
| 受信シグネチャログ | `passiveBearings[]` を拡張 |
| 解析モーダル起動 | `sig-canvas` にタッチイベント追加 |
| シグネチャ種別判定精度 | `aiPrecision.sensor` × `genAlloc.ai` をそのまま流用 |
| 精度円・ゴースト描画 | `contactCandidates` の描画ロジックを転用 |
| マップ外縁の度数目盛り | `drawMinimap()` に追加 |
| 経時劣化 | `passiveBearings` の `life` フェードと同機構 |

#### G. 未確定（実装前に要決定）
- 解析モーダルを開いている間の移動ペナルティ有無
- ゴーストの針路変更検知タイムラグ（すぐズレるか、数秒のバッファを持つか）
- 1画面に同時表示できる解析結果サークルの上限数
- デコイを解析してゴーストが出た場合のプレイヤーへのフィードバック（現状は「わからない」が正しい）

---

### 4-5. 🧊 中長期（未着手）

---

## 5. 実装優先度（最新）

> 旧優先度リスト（CLAUDE/MEMORY）は**#2〜#6が実は実装済み**で陳腐化していた（PR#34-38で消化）。以下に再編。

### 消化済み（PR#34-41）
- ✅ 有視界システム（#34）/ 自動ロックオン+攻撃ON/OFF+射程サークル / 武器リロード・マガジン+GEN発射レート
- ✅ パッシブ方位ウェッジ "運ゲー"解消（#35）/ センサー制約型敵AI 基本（#35）/ ソナー残像（`sonar-fill`）
- ✅ 艦種別シルエット+エンジン噴射色（#36）/ スプライト+DPR（#37）/ 星雲パララックス（#38）
- ✅ 想定ロックオン完成（デバフ#39→#40 / beam長射程狙撃・命中ジッター・レティクル色分け＝PR#41）
- ✅ ノード・構造物スプライト（#39取込→#40）
- ✅ 敵AI適応戦略テーブル `predictedBehavior`（PR#41）
- ✅ 潜航型ジャミング3種（範囲/継続EM/EMパルス、PR#41）
- ✅ 潜航型デコイ射出（ミサイル誘引、PR#41）/ 積載量確定（空母6/潜航3/攻撃2）

### 次に実装すべき順（未消化）
1. ✅ 空母型ドローン4種（§3-6）/ ✅ 建設物4種（§3-7 全種実装済・PR#51）
2. ✅ AI精度スライダー(§3-2) ＋ ✅AIロックオン候補表示(§3-3) — 実装済
3. ✅ アップグレードツリー再整合（PR#53）— §3-1
4. ✅ ミサイル2タイプ（§3-10・PR#51）/ ✅ 修復ドローン（§3-9・PR#51）
5. ✅ シグネチャtrail全センサー一般化 — §3-12 残施策（PR#52）
6. 🚧 マップ4層構造 + 地形シグネチャ要素（ビジュアル改良）— §3-13。**Phase1=✅** / **Phase2=✅** → 残: 実機30fps計測 → Phase3=熱雲/4種目は計測後に判断

---

### Higgsfield ビジュアルエフェクト（PR#57・PR#58 完了）
- ✅ **Phase1（PR#57）**: `fx_explosion_big/small`, `fx_kinetic_flash`, `fx_beam_impact`, `fx_thruster_jet`, `fx_missile_exhaust` — 爆発・弾着・スラスター・ミサイル排気スプライト
- ✅ **Phase2（PR#58）**: `drone_attack/scout/decoy/missile/turret/buoy`, `fx_bolt_player/enemy`, `fx_decoy`, `particle_heat/optic/higgs` — ドローン全種・弾体・デコイ・センサーtrail粒子スプライト
- **手法**: 黒背景 + `globalCompositeOperation='lighter'` 加算合成。`spriteReady()` ガード＋キャンバスフォールバック。`shadowBlur` 2箇所も合わせて除去。
- **残**: 岩礁（debris）スポーン岩の置き換え（現状はキャンバス四角形）/ 熱雲Blobのテクスチャ化 — 任意拡張。

---

*最終更新: 2026-06-15 — PR#76+77で重慣性物理・武器射角・フリーファイア・後方被弾ボーナス・デモモード・敵艦種選択を実装。スプライトRound3全面再生成・fx_beam_main.png新規生成。全壁打ちタスク実装完了。残は任意拡張のみ。*
